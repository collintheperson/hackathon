{{#partial "content"}}

    <p>Content:</p>

    <p>{{ teams.name}}</p>
    <h5>Created At:</h5>
    <p> {{ teams.description }}</p>
    <h5>Post status:</h5>
    <p>
        {{#if team.published }}
            <span class="glyphicon glyphicon-star" aria-hidden="true"></span>  Published
        {{else}}
            <span class="glyphicon glyphicon-pencil" aria-hidden="true"></span> Draft
        {{/if}}

        <a href="/posts/{{team.id}}/update">Edit this team</a><br>
        <a href="/posts/{{team.id}}/delete">DELETE this team (no undo)</a>
    </p>

{{/partial}}

{{> layout.hbs}}