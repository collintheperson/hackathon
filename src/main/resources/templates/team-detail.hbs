{{#partial "content"}}

    <p>Content:</p>

    <p>{{ team.content }}</p>
    <h5>Created At:</h5>
    <p> {{ team.createdAt }}</p>
    <h5>Post status:</h5>
    <p>
        {{#if team.published }}
            <span class="glyphicon glyphicon-star" aria-hidden="true"></span>  Published
        {{else}}
            <span class="glyphicon glyphicon-pencil" aria-hidden="true"></span> Draft
        {{/if}}

        <a href="/posts/{{team.id}}/update">Edit this post</a><br>
        <a href="/posts/{{team.id}}/delete">DELETE this post (no undo)</a>
    </p>

{{/partial}}

{{> layout.hbs}}